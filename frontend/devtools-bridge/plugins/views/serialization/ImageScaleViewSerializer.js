import ImageViewSerializer from './ImageViewSerializer';

export default class ImageScaleViewSerializer extends ImageViewSerializer {
  // For now it's pretty much the same as `ImageViewSerializer`. Scale method
  // and other properties will be added later.
}
